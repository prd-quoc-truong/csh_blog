{
  "name": "site",
  "version": "1.0.0",
  "description": "`` cd src/static npm install --global gulp npm install --save-dev gulp cd site gulp ```",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1"
  }
}
